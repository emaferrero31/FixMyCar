{
  "name": "taller-mecanico",
  "private": true,
  "scripts": {
    "start": "concurrently -n backend,frontend -c blue,green \"npm run backend\" \"npm run frontend\"",
    "backend": "cd backend && mvnw.cmd spring-boot:run",
    "frontend": "live-server frontend --port=5500 --open=/index.html"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "live-server": "^1.2.2"
  }
}
