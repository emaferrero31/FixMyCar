spring:
  datasource:
    url: jdbc:postgresql://aws-1-us-west-2.pooler.supabase.com:5432/postgres
    username: postgres.wgreghgejcwzkxoljklx
    password: Emanuel2002Fer..
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
  # Correo (SMTP). Para Gmail usar contraseña de aplicación.
  mail:
    host: smtp.gmail.com
    port: 587
    username: tu-correo@gmail.com
    password: tu-contraseña-de-aplicacion
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

server:
  port: 8080
  servlet:
    context-path: /api
  # Refuerzo: el cuerpo JSON lo limita el Connector (ver TomcatConnectorConfig)
  tomcat:
    max-http-form-post-size: 40MB
    max-swallow-size: 40MB

# URL del frontend (enlace de recuperar contraseña en el correo)
app:
  frontend-url: https://fixmycar-1-yul7.onrender.com
  mail:
    from: noreply@fixmycar.com
  # Talleres demo para probar el listado cliente (ver application-dev.yml; contraseña Demo1234)
  seed-demo-talleres: false
